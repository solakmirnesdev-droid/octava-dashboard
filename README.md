# Octava Dashboard

Internal tool where workers enter songs, place chords over lyrics, and publish.

```bash
npm install
npm run dev
```

Runs on `http://localhost:8000`, proxies `/api` to the backend on `4000`.
